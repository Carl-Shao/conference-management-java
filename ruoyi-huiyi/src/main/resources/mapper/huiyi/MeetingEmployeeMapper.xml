<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.huiyi.mapper.MeetingEmployeeMapper">
    
    <resultMap type="MeetingEmployee" id="MeetingEmployeeResult">
        <result property="id"    column="id"    />
        <result property="employeeNo"    column="employee_no"    />
        <result property="employeeName"    column="employee_name"    />
        <result property="department"    column="department"    />
        <result property="position"    column="position"    />
        <result property="email"    column="email"    />
        <result property="phone"    column="phone"    />
        <result property="role"    column="role"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="userId"    column="user_id"    />
    </resultMap>

    <sql id="selectMeetingEmployeeVo">
        select id, employee_no, employee_name, department, position, email, phone, role, create_time, update_time, user_id from meeting_employee
    </sql>

    <select id="selectMeetingEmployeeList" parameterType="MeetingEmployee" resultMap="MeetingEmployeeResult">
        <include refid="selectMeetingEmployeeVo"/>
        <where>  
            <if test="employeeNo != null  and employeeNo != ''"> and employee_no = #{employeeNo}</if>
            <if test="employeeName != null  and employeeName != ''"> and employee_name like concat('%', #{employeeName}, '%')</if>
            <if test="department != null  and department != ''"> and department = #{department}</if>
            <if test="position != null  and position != ''"> and position = #{position}</if>
            <if test="email != null  and email != ''"> and email = #{email}</if>
            <if test="phone != null  and phone != ''"> and phone = #{phone}</if>
            <if test="role != null  and role != ''"> and role = #{role}</if>
        </where>
    </select>
    
    <select id="selectMeetingEmployeeById" parameterType="Long" resultMap="MeetingEmployeeResult">
        <include refid="selectMeetingEmployeeVo"/>
        where id = #{id}
    </select>

    <select id="checkEmployeeNoUnique" parameterType="String" resultMap="MeetingEmployeeResult">
        <include refid="selectMeetingEmployeeVo"/>
        where employee_no = #{employeeNo}
    </select>

    <insert id="insertMeetingEmployee" parameterType="MeetingEmployee" useGeneratedKeys="true" keyProperty="id">
        insert into meeting_employee
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="employeeNo != null and employeeNo != ''">employee_no,</if>
            <if test="employeeName != null and employeeName != ''">employee_name,</if>
            <if test="department != null">department,</if>
            <if test="position != null">position,</if>
            <if test="email != null">email,</if>
            <if test="phone != null">phone,</if>
            <if test="role != null and role != ''">role,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="userId != null">user_id,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="employeeNo != null and employeeNo != ''">#{employeeNo},</if>
            <if test="employeeName != null and employeeName != ''">#{employeeName},</if>
            <if test="department != null">#{department},</if>
            <if test="position != null">#{position},</if>
            <if test="email != null">#{email},</if>
            <if test="phone != null">#{phone},</if>
            <if test="role != null and role != ''">#{role},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="userId != null">#{userId},</if>
         </trim>
    </insert>

    <update id="updateMeetingEmployee" parameterType="MeetingEmployee">
        update meeting_employee
        <trim prefix="SET" suffixOverrides=",">
            <if test="employeeNo != null and employeeNo != ''">employee_no = #{employeeNo},</if>
            <if test="employeeName != null and employeeName != ''">employee_name = #{employeeName},</if>
            <if test="department != null">department = #{department},</if>
            <if test="position != null">position = #{position},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="role != null and role != ''">role = #{role},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="userId != null">user_id = #{userId},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteMeetingEmployeeById" parameterType="Long">
        delete from meeting_employee where id = #{id}
    </delete>

    <delete id="deleteMeetingEmployeeByIds" parameterType="String">
        delete from meeting_employee where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>