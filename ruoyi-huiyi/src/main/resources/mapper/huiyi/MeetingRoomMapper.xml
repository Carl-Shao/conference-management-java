<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.huiyi.mapper.MeetingRoomMapper">
    
    <resultMap type="MeetingRoom" id="MeetingRoomResult">
        <result property="id"    column="id"    />
        <result property="roomName"    column="room_name"    />
        <result property="location"    column="location"    />
        <result property="capacity"    column="capacity"    />
        <result property="equipment"    column="equipment"    />
        <result property="rtspUrl"    column="rtsp_url"    />
        <result property="status"    column="status"    />
        <result property="sortOrder"    column="sort_order"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectMeetingRoomVo">
        select id, room_name, location, capacity, equipment, rtsp_url, status, sort_order, create_time, update_time from meeting_room
    </sql>

    <select id="selectMeetingRoomList" parameterType="MeetingRoom" resultMap="MeetingRoomResult">
        <include refid="selectMeetingRoomVo"/>
        <where>  
            <if test="roomName != null  and roomName != ''"> and room_name like concat('%', #{roomName}, '%')</if>
            <if test="location != null  and location != ''"> and location = #{location}</if>
            <if test="capacity != null "> and capacity = #{capacity}</if>
            <if test="equipment != null  and equipment != ''"> and equipment = #{equipment}</if>
            <if test="rtspUrl != null  and rtspUrl != ''"> and rtsp_url = #{rtspUrl}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
            <if test="sortOrder != null "> and sort_order = #{sortOrder}</if>
        </where>
    </select>
    
    <select id="selectMeetingRoomById" parameterType="Long" resultMap="MeetingRoomResult">
        <include refid="selectMeetingRoomVo"/>
        where id = #{id}
    </select>

    <select id="selectMeetingRoomByIdForUpdate" parameterType="Long" resultMap="MeetingRoomResult">
        <include refid="selectMeetingRoomVo"/>
        where id = #{id}
        FOR UPDATE
    </select>

    <insert id="insertMeetingRoom" parameterType="MeetingRoom" useGeneratedKeys="true" keyProperty="id">
        insert into meeting_room
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="roomName != null and roomName != ''">room_name,</if>
            <if test="location != null">location,</if>
            <if test="capacity != null">capacity,</if>
            <if test="equipment != null">equipment,</if>
            <if test="rtspUrl != null">rtsp_url,</if>
            <if test="status != null">status,</if>
            <if test="sortOrder != null">sort_order,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="roomName != null and roomName != ''">#{roomName},</if>
            <if test="location != null">#{location},</if>
            <if test="capacity != null">#{capacity},</if>
            <if test="equipment != null">#{equipment},</if>
            <if test="rtspUrl != null">#{rtspUrl},</if>
            <if test="status != null">#{status},</if>
            <if test="sortOrder != null">#{sortOrder},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateMeetingRoom" parameterType="MeetingRoom">
        update meeting_room
        <trim prefix="SET" suffixOverrides=",">
            <if test="roomName != null and roomName != ''">room_name = #{roomName},</if>
            <if test="location != null">location = #{location},</if>
            <if test="capacity != null">capacity = #{capacity},</if>
            <if test="equipment != null">equipment = #{equipment},</if>
            <if test="rtspUrl != null">rtsp_url = #{rtspUrl},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteMeetingRoomById" parameterType="Long">
        delete from meeting_room where id = #{id}
    </delete>

    <delete id="deleteMeetingRoomByIds" parameterType="String">
        delete from meeting_room where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>